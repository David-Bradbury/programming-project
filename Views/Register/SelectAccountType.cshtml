@model RegisterViewModel


@{
    ViewData["Title"] = "Register";
}
            <div class="form-group">
                                <a class="btn btn-primary" asp-action="Register" asp-route-id="1">Owner</a>
            </div>            
            <div class="form-group">
                 <a class="btn btn-primary" asp-action="Register" asp-route-id="2">Walker</a>
            </div>