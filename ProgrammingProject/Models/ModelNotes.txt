Just jotting down ideas:

-Possibly worth looking into whether we sepearate a listing into requests and offers.

- Need to create Password regex for valid passwords

- Need to change address form or registerviewmodel and perhaps change to autocomplete complete address using google api

- Need to fix mobile number regex

- Maybe import db with all australian postcodes and instead of using regex check postcode against db entries

